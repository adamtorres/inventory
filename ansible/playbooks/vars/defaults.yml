# These should be values that could apply to any role.  There should be nothing that would only apply to a single role.
# These are for Debian/Linux only and specifically Raspbian.

default_user: pi
default_group: pi
default_home_dir: /home/pi
default_app_dir: /srv

python_virtualenv_command: /usr/bin/python -m venv
